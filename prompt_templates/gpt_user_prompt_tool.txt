PROMPT:


# STEP 1: 
As your first step yu must carefully read the CONTEXT. 
Then, decide if the CONTEXT describes an impact or damage to critical infrastructure caused by a natural hazard.  
Determine if the CONTEXT also mentions the concrete location of each affected critical infrastructure asset.


Your Decision:  
- If the CONTEXT describes an actual damage to critical infrastructure and mentions also its location, proceed with STEP 2.  
- If the CONTEXT does NOT describe a actual damage to critical infrastructure, STOP here. Do not proceed. Your final OUTPUT must be an empty JSON object.
- If the CONTEXT describes only a natural hazard event without describing an actual damage to critical infrastructure, then STOP here. Do not proceed. Your final OUTPUT must be an empty JSON object.



# STEP 2:
Provide, in the field "infrastructure_type", information about the affected critical infrastructure assets mentioned in the CONTEXT.
For each case, you should determine if the CONTEXT mentions that the respective critical infrastructure asset was (directly or indirectly) affected by a natural hazard.
Use the tool "get_infrastructure_entities"  which returns a list of critical infrastructure (ci) entities mentioned in the CONTEXT. It can help you to detect infrastructure assets mentioned in the text.
If this is the case, then write the affected infrastructure to the field "infrastructure_type" (example: "infrastructure_type": "power plant"). 




Proceed with STEP 3.



# STEP 3: 
When you found a case of hazard-affected critical infrastructure, then find the location of this infrastructure.
Provide your response for the location in the field "location".
Try to use one to three words to describe each location of the affected infrastructure, being as specific as possible.
If no information about the location of the affected infrastructure is found, then return a "NAN" value for this field. 
DON'T create new items about the location that weren’t there.


Proceed with STEP 4.



# STEP 4:
This is your final step. 
For each case of affected critical infrastructure, its damage type and location, you should return a single object. 
In case multiple cases exists, you should create an array of objects.
Provide your final response below the section "ANSWER".




**Input**:

QUESTION: 
"Which infrastructure failures are mentioned in the text? Categorize the output by the type of infrastructure, the location, its location type (e.g., region, county, city, river etc.), the type of damage and potentially quantitative information about the damage."


CONTEXT:
{% for item in context %}
- ("text": "{{ item["text"] }}")
{% endfor %}




**EXAMPLE:** OUTPUT schema
Example of a separate object:
- ("infrastructure_type": "power plant" )
- ("location": "New York" )
- ("location_type": "city" )
- ("damage": "disrupted" )
- ("damage_value": "NAN" )


**ANSWER**: 
Provide here your response in the given OUTPUT format.
